server:
  port: 8081
---
spring:
  application:
    name: ms-user
  config:
    import: optional:configserver:http://localhost:8888/
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
