spring:
  application:
    name: ms-user
---
server:
  port: 8082
---
spring:
  profiles:
    active: dev
---
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
