spring:
  application:
    name: ms-mail
---
server:
  port: 8083
---
spring:
  profiles:
    active: dev
---
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server